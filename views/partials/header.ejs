<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>simple-auth</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.1.3/css/bootstrap.min.css"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/ionicons/2.0.1/css/ionicons.min.css"
    />
    <link rel="stylesheet" href="index.css" />
  </head>

  <body>
    <% if (page!=="login") { %> 
    <nav class="navbar navbar-light bg-light justify-content-between">
      <div>
        <i class="icon ion-person" class="pr-3"></i>
        <a class="pr-3"><%= user.username %> </a>
      </div>
      <div>
        <% if (['admin'].indexOf(page)>=0) { %>
          <a href="/" class="btn btn-primary">Voltar</a>
          <% } 
          if (['projects'].indexOf(page)>=0) { 
            if (user.roles.filter((role)=>['root','admin'].indexOf(role)>=0).length) { %>
        <a href="/admin" class="btn btn-primary">Admin</a>
        <% }} %>
        <a href="/logout" class="btn btn-primary">Sair</a>
      </div>
    </nav>
    <% } %> 